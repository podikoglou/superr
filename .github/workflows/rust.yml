name: Rust

on:
  push:
    branches: [ "main", "qua" ]
  pull_request:
    branches: [ "main", "qua" ]

env:
  CARGO_TERM_COLOR: always

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: Run tests
      run: cargo test --verbose
